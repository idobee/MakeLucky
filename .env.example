# Example environment variables for MakeLucky (do NOT commit real secrets)
# Copy this file to `.env.local` and fill with your values.

# Google AdSense Publisher ID (public identifier but managed via env for convenience)
VITE_GOOGLE_ADSENSE_CLIENT_ID="ca-pub-xxxxxxxxxxxxxxxx"
VITE_GOOGLE_ADSENSE_SLOT_ID="0000000000" # optional; your ad slot id (numeric)

# If you later add a server-side proxy, do NOT put API keys in the frontend.
# GEMINI_API_KEY=use-server-not-frontend
